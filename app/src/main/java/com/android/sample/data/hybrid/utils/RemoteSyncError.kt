package com.android.sample.data.hybrid.utils

/** Enumeration of possible remote synchronization errors. */
enum class RemoteSyncError {
  REMOTE_INSERT_FAILED,
  REMOTE_UPDATE_FAILED,
  REMOTE_DELETE_FAILED,
  REMOTE_GET_FAILED
}
